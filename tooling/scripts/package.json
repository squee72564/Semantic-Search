{
  "name": "@repo/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:runtime": "node src/check-runtime.ts",
    "test": "node --test run-with-env.test.mjs",
    "typecheck": "tsc -b",
    "lint": "oxlint . --report-unused-disable-directives",
    "lint:types": "oxlint --type-aware ."
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
